# Kirby KQL API URL (production uses basic auth, local dev is open)
KIRBY_API_URL=https://cms.emintoyland.com

# Only needed if KQL auth is enabled in Kirby config
KIRBY_USER=api-user
KIRBY_PASS=your-password-here
